# Local config and secrets (copy .env from .env.example)
.env
api-key.txt

# Python virtualenv
venv/

# Former manual build source; the build now fetches a pinned commit itself, so it can be deleted
repo/

# Generated graph output (built by the host-side watcher)
graphify-out/

# Runtime watcher state and logs
.watchers/
healthcheck.log

# Compiled helper binary (obtained separately)
bin/

# Python cache
__pycache__/
*.pyc
